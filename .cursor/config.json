{
    "rules": [
      {
        "pattern": ".*",
        "instructions": "NEVER run commands yourself to start or stop services. Instead, only tell the user on which commands to run manually. For now, everything is being run locally, including our db, Supabase."
      }
    ]
  }